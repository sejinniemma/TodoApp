import React from 'react';

const Header = (props) => {
  return (
    <div>
      <button>🌓</button>
      <span>All</span>
      <span>Active</span>
      <span>Completed</span>
    </div>
  );
};

export default Header;
